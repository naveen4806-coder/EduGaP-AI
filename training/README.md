# EdUGaP AI - Student Academic Risk MLP Classification Pipeline

This directory contains the complete PyTorch machine learning pipeline for predicting student academic risk categories (`Low Risk`, `Medium Risk`, `High Risk`) to empower proactive educational interventions.

## Architecture
- **Model Type**: 4-Layer Multilayer Perceptron (MLP)
- **Input Dimension**: 12 standardized student behavioral and academic features
- **Hidden Layers**: 64 -> 32 -> 16 neurons with ReLU activations and 0.25 Dropout regularization
- **Output Layer**: 3 classes with CrossEntropyLoss and class-frequency inverse weighting
- **Optimizer**: Adam (lr=0.001) with validation loss early stopping

## Input Features (Zero PII Guarantee)
1. `previous_assessment_avg`: Historical assessment average score (0-100)
2. `recent_assessment_score`: Score on the most recent assessment (0-100)
3. `topic_wise_accuracy`: Aggregate topic accuracy percentage (0-100)
4. `quiz_completion_rate`: Proportion of assigned quizzes completed (0-100)
5. `attendance_percentage`: Classroom attendance percentage (0-100)
6. `assignment_completion_rate`: Assignment submission rate (0-100)
7. `study_session_completion_rate`: Adherence to recommended study schedule (0-100)
8. `avg_daily_study_time_minutes`: Average self-study duration per day
9. `late_submissions_count`: Total assignments submitted past the deadline
10. `integrity_events_count`: Anti-cheat integrity flags detected
11. `weak_topics_count`: Number of topics below mastery threshold
12. `assessment_attempt_count`: Total assessment attempts logged

*Note: In accordance with academic ethics and privacy standards, personally identifiable information (PII) such as student name, gender, race, or address is strictly excluded from feature extraction and model inputs.*

## Directory Structure
```
training/
├── data/                  # Generated and split benchmark datasets
├── models/                # Saved PyTorch checkpoint, scaler, and evaluation report
├── preprocess.py          # Data generation, leakage removal, scaling, splits
├── train_mlp.py           # PyTorch MLP architecture and training loop
├── evaluate.py            # Accuracy, macro F1, confusion matrix, calibration
├── inference.py           # FastAPI prediction service (POST /ml/predict-risk)
└── requirements.txt       # Python dependencies
```

## Setup and Execution Commands

```bash
# 1. Navigate to training directory
cd training

# 2. (Optional) Create and activate virtual environment
python -m venv .venv
# On Windows:
.venv\Scripts\activate
# On Linux/macOS:
source .venv/bin/activate

# 3. Install dependencies
pip install -r requirements.txt

# 4. Preprocess data and generate train/val/test splits
python preprocess.py

# 5. Train the PyTorch MLP model
python train_mlp.py

# 6. Evaluate on test set
python evaluate.py

# 7. Start the FastAPI inference microservice
uvicorn inference:app --reload --port 8001
```

## API Endpoint Reference

### `POST /ml/predict-risk`
**Request Body**:
```json
{
  "previous_assessment_avg": 78.5,
  "recent_assessment_score": 82.0,
  "topic_wise_accuracy": 80.0,
  "quiz_completion_rate": 95.0,
  "attendance_percentage": 94.0,
  "assignment_completion_rate": 90.0,
  "study_session_completion_rate": 85.0,
  "avg_daily_study_time_minutes": 75.0,
  "late_submissions_count": 0,
  "integrity_events_count": 0,
  "weak_topics_count": 1,
  "assessment_attempt_count": 6
}
```

**Response**:
```json
{
  "risk_level": "Low Risk",
  "confidence_score": 0.885,
  "probabilities": {
    "Low Risk": 0.885,
    "Medium Risk": 0.095,
    "High Risk": 0.02
  },
  "model_version": "v1.0-mlp",
  "fallback_used": false,
  "explanation_factors": [
    "Strong assessment scores and consistent attendance",
    "High study session completion rate"
  ],
  "model_limitation_notice": "Model Limitation Notice: Risk classifications generated by EdUGaP AI are advisory screening indicators intended exclusively to support proactive educator mentorship..."
}
```

## Model Limitation & Ethical AI Notice
EdUGaP AI model predictions serve strictly as advisory decision-support signals for teachers and academic advisors. Predictions are never used to automatically compute report card grades, initiate disciplinary penalties, or dictate admission/dismissal outcomes.
